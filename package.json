{
  "name": "job-tracker",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run build -w @job-tracker/shared && concurrently -n shared,api,web -c gray,blue,green \"npm run dev -w @job-tracker/shared\" \"npm run dev -w @job-tracker/api\" \"npm run dev -w @job-tracker/web\"",
    "build": "npm run build -w @job-tracker/shared && npm run build -w @job-tracker/api && npm run build -w @job-tracker/web",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
